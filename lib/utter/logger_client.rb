require 'logger'
# Utter Logging Server
# Even though that utter can have several api servers connecting to several redis servers, it should only have one centeral logging server
#
module Utter
  class Logger < Logger
    #TODO utter logserver start
  end
end
